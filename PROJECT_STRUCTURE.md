# AI 聊天应用 - 项目结构说明

## 📁 项目目录结构

```
antdx-demo/
├── src/
│   ├── components/          # 组件目录
│   │   └── AIChat/         # AI聊天相关组件
│   │       ├── ChatMessage/      # 聊天消息组件
│   │       │   ├── index.tsx
│   │       │   └── style.css
│   │       ├── ChatInput/        # 输入框组件
│   │       │   ├── index.tsx
│   │       │   └── style.css
│   │       ├── ModelSelector/    # 模型选择器组件
│   │       │   ├── index.tsx
│   │       │   └── style.css
│   │       ├── ChatHeader/       # 聊天头部组件
│   │       │   ├── index.tsx
│   │       │   └── style.css
│   │       ├── ChatContainer/    # 聊天容器组件
│   │       │   ├── index.tsx
│   │       │   └── style.css
│   │       └── index.ts          # 组件统一导出
│   │
│   ├── pages/              # 页面目录
│   │   ├── AIChatPage/     # AI聊天页面
│   │   │   ├── index.tsx
│   │   │   └── style.css
│   │   └── index.ts
│   │
│   ├── services/           # 服务层（API调用）
│   │   ├── chatService.ts  # 聊天服务
│   │   └── index.ts
│   │
│   ├── hooks/              # 自定义Hooks
│   │   ├── useChat.ts      # 聊天功能Hook
│   │   └── index.ts
│   │
│   ├── types/              # TypeScript类型定义
│   │   ├── chat.ts         # 聊天相关类型
│   │   └── index.ts
│   │
│   ├── constants/          # 常量配置
│   │   ├── models.ts       # AI模型配置
│   │   └── index.ts
│   │
│   ├── utils/              # 工具函数
│   │   ├── request.ts      # HTTP请求封装
│   │   ├── storage.ts      # 本地存储工具
│   │   └── index.ts
│   │
│   ├── App.tsx             # 应用主入口
│   ├── App.css             # 应用全局样式
│   ├── main.tsx            # React入口
│   └── index.css           # 全局基础样式
│
├── public/                 # 静态资源
├── package.json            # 项目配置
├── vite.config.ts          # Vite配置
├── tsconfig.json           # TypeScript配置
└── README.md               # 项目说明
```

## 🏗️ 架构设计

### 1. 组件层（Components）
- **ChatMessage**: 单条消息展示，支持用户/AI消息样式区分
- **ChatInput**: 消息输入框，支持多行输入、发送、停止等功能
- **ModelSelector**: AI模型选择器，支持切换不同模型
- **ChatHeader**: 页面头部，显示标题和消息统计
- **ChatContainer**: 消息容器，负责消息列表展示和滚动

### 2. 页面层（Pages）
- **AIChatPage**: AI聊天主页面，整合所有组件

### 3. 业务逻辑层（Hooks）
- **useChat**: 封装聊天核心逻辑
  - 消息发送
  - 消息重发
  - 消息删除
  - 清空对话
  - 停止生成

### 4. 服务层（Services）
- **chatService**: AI聊天API服务
  - 发送消息（普通/流式）
  - 获取模型列表
  - 当前使用模拟数据，可替换为真实API

### 5. 工具层（Utils）
- **request**: HTTP请求封装（支持超时、错误处理）
- **storage**: 本地存储工具
- **通用工具**: ID生成、时间格式化、防抖节流等

### 6. 类型层（Types）
- 完整的TypeScript类型定义
- 消息、模型、会话等核心类型

### 7. 常量层（Constants）
- AI模型配置列表
- 默认配置参数

## 🎨 技术栈

- **React 19.2**: UI框架
- **TypeScript**: 类型安全
- **Ant Design 6.x**: UI组件库
- **Vite 6.x**: 构建工具
- **CSS Modules**: 样式隔离

## 🚀 快速开始

### 安装依赖
```bash
pnpm install
```

### 启动开发服务器
```bash
pnpm dev
```

### 构建生产版本
```bash
pnpm build
```

### 预览生产版本
```bash
pnpm preview
```

## 📝 功能特性

### 已实现功能
- ✅ 实时对话（模拟流式响应）
- ✅ 多模型切换
- ✅ 消息重发
- ✅ 消息删除
- ✅ 清空对话
- ✅ 停止生成
- ✅ 消息复制
- ✅ 错误处理
- ✅ 响应式设计
- ✅ 打字机效果

### 待扩展功能
- ⬜ 对话历史持久化
- ⬜ 多会话管理
- ⬜ 导出对话记录
- ⬜ 语音输入
- ⬜ 文件上传
- ⬜ Markdown渲染
- ⬜ 代码高亮
- ⬜ 主题切换（暗色模式）
- ⬜ 国际化支持

## 🔧 配置说明

### 路径别名
项目配置了 `@` 作为 `src` 目录的别名：

```typescript
// vite.config.ts
resolve: {
  alias: {
    '@': path.resolve(__dirname, './src'),
  },
}

// tsconfig.app.json
"paths": {
  "@/*": ["src/*"]
}
```

使用示例：
```typescript
import { useChat } from '@/hooks';
import type { Message } from '@/types';
```

### API集成
当前使用模拟数据，实际项目中需要替换 `src/services/chatService.ts` 中的API调用：

```typescript
// 替换为真实API
async sendMessage(request: ChatRequest): Promise<ChatResponse> {
  return await httpClient.post<ChatResponse>('/chat/completions', request);
}
```

## 📦 代码规范

### 文件命名
- 组件文件：PascalCase（如：`ChatMessage/index.tsx`）
- 工具文件：camelCase（如：`request.ts`）
- 类型文件：camelCase（如：`chat.ts`）
- 样式文件：kebab-case或与组件同名（如：`style.css`）

### 导入顺序
1. React相关
2. 第三方库
3. 项目内部模块（使用@别名）
4. 类型导入
5. 样式文件

### 组件结构
```typescript
/**
 * 组件说明
 */

import React from 'react';
// ... 其他导入

interface ComponentProps {
  // props定义
}

export const Component: React.FC<ComponentProps> = (props) => {
  // 组件逻辑
  return (
    // JSX
  );
};

export default Component;
```

## 🎯 最佳实践

1. **组件拆分**: 每个组件职责单一，易于维护
2. **类型安全**: 完整的TypeScript类型定义
3. **状态管理**: 使用自定义Hook封装业务逻辑
4. **样式隔离**: 每个组件独立样式文件
5. **错误处理**: 完善的错误提示和处理机制
6. **性能优化**: 使用useCallback、useMemo等优化性能
7. **代码复用**: 工具函数和通用组件抽离

## 📄 许可证

MIT

