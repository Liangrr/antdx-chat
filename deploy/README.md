# 自动部署脚本 🚀

一键构建并部署 Vue 项目到云服务器。

## 快速开始

```bash
# 在项目根目录执行
pnpm deploy
```

## 功能特性

- ✅ 自动构建项目
- ✅ 自动压缩上传
- ✅ 自动备份原文件
- ✅ 自动解压部署
- ✅ 自动清理旧备份（保留最近10个）
- ✅ 彩色日志输出

## 配置说明

### 首次使用

1. **复制配置文件**
   ```bash
   cd deploy
   cp deploy.config.example.cjs deploy.config.cjs
   ```

2. **修改配置**
   编辑 `deploy.config.cjs`，填入你的服务器信息：
   ```javascript
   server: {
       host: '你的服务器IP',
       port: 22,
       username: 'root',
       password: '你的密码',
   }
   ```

3. **确保服务器已安装 unzip**
   ```bash
   # CentOS/RHEL
   yum install -y unzip
   
   # Ubuntu/Debian
   apt-get install -y unzip
   ```

## 使用方法

### 1. 完整部署（构建+上传）
```bash
pnpm deploy
```

### 2. 只上传不构建
修改 `deploy.config.cjs`：
```javascript
build: {
    skipBuild: true,
}
```

## 部署流程

```
构建项目 → 压缩 → 连接服务器 → 上传 → 备份 → 解压 → 清理 → 完成
```

## 服务器目录结构

```
/usr/project/pure/
├── dist/              # 当前生产环境
└── backups/           # 历史备份
    ├── dist20251025140250/
    ├── dist20251025150330/
    └── ...
```

## 回滚操作

```bash
# 查看备份
ssh root@your-server "ls -lh /usr/project/pure/backups"

# 回滚到某个备份
ssh root@your-server
cd /usr/project/pure
rm -rf dist
cp -r backups/dist20251025140250 dist
```

## 安全提示

⚠️ `deploy.config.cjs` 包含服务器密码，已在 `.gitignore` 中排除，**不会被提交到 Git**。

推荐使用 SSH 密钥代替密码认证。

## 故障排查

### 连接失败
- 检查服务器 IP、端口、用户名、密码
- 确保服务器 SSH 服务已启动
- 检查防火墙是否开放 22 端口

### 构建失败
- 先手动执行 `pnpm build` 测试
- 检查 Node.js 版本是否符合要求

### 上传失败
- 检查服务器磁盘空间
- 确保远程目录有写入权限

### 解压失败
- 确保服务器已安装 unzip

---

**最后更新**: 2025-10-25

